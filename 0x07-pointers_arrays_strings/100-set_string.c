#include "main.h"

/**
 * set_string - sets the value of a pointer to a char
 * @s: sets pointer to a pointer to a char
 * @to: sets pointer to a char
 *
 * Return: 0
 */

void set_string(char **s, char *to)
{
*s = to;
}
